# STACK — <app or org name>

> Generated by `/setup-deslop` on YYYY-MM-DD. House overlay telling the audit what each stack-agnostic category means in your codebase. Re-run `/setup-deslop` when the stack changes.

## Stack summary

| Layer           | Choice                                                |
|-----------------|-------------------------------------------------------|
| Frontend        | <e.g. React + Vite>                                   |
| Backend / API   | <e.g. RPC server functions, REST routes, GraphQL>     |
| Database(s)     | <e.g. one document store; or relational + cache>      |
| Auth provider   | <e.g. Clerk, Auth0, NextAuth, self-managed>           |
| Payment         | <e.g. Stripe webhooks; or "none">                     |
| Paid per-call   | <e.g. LLM API, SMS, email; or "none">                 |
| Hosting/deploy  | <e.g. Vercel + Convex cloud; or self-hosted>          |

## Audit categories (the overlay)

| Category          | What it means here                                                            | Audit target                                                |
|-------------------|-------------------------------------------------------------------------------|-------------------------------------------------------------|
| Secrets           | <where secrets live; client/server env convention; bundle-leak surface>       | <how to check (e.g. grep `dist/` for non-prefixed names)>   |
| Authentication    | <the safe accessor for identity; the "never from args/body" tell>             | <enumerate handlers that read identity from request body>   |
| Tenant scoping    | <tenant unit; tenant key field; the **scoping seam** every data path uses>    | <enumerate data paths NOT routed through the seam>          |
| Authorisation     | <where role checks live; supervisor-only mechanism>                           | <enumerate destructive ops without role check>              |
| Internal surface  | <what marks an endpoint not-public; where debug/admin/seed routes hide>       | <enumerate sensitive ops still on the public tier>          |
| Input validation  | <validation library + boundary; injection risk surface>                       | <enumerate `v.any()` / loose args; raw SQL>                 |
| Cost & abuse      | <paid-call surface; rate-limit/entitlement layer; spend caps>                 | <enumerate paid endpoints without auth or rate limit>       |
| Data integrity    | <idempotency mechanism; transaction/atomic-op story; webhook signature>       | <enumerate money/entitlement paths lacking idempotency>     |
| Ops               | <backups+restore; prod/dev split; rollback; error tracking; structured logs>  | <verify-before-ship items the gate will check>              |

## Gate defaults for this stack

- (stack-specific must-be-true items to fold into GATE.md beyond the standard line — e.g. "Convex prod's `CLERK_JWT_ISSUER_DOMAIN` is a Clerk **production** instance, not `*.clerk.accounts.dev`")

## Unknowns (resolve during audit)

- (anything the interview couldn't confirm — e.g. "`R2_PUBLIC_URL` setting in prod env", "`CLERK_JWT_ISSUER_DOMAIN` in prod env", "backup restore-test evidence", "error-tracking provider in code")
