# STACK - <app or org name>

> Generated by `/setup-deslop`. This is *your* house overlay: it tells the audit what each
> stack-agnostic category means in your codebase. Keep it in the repo (or a canonical org copy
> you drop into every project). Re-run `/setup-deslop` when your stack changes.

## Stack summary
- Frontend: <…>
- Backend / API style: <REST routes | RPC/server functions | GraphQL | server actions | …>
- Database(s): <…>
- Auth provider: <…>
- Payment processor: <…>
- Paid per-request APIs: <LLM / SMS / email / …>
- Hosting / deploy: <…>

## Secrets
- Secrets live in: <env / secret manager / dashboard>
- Client/server env convention: <e.g. PUBLIC_ prefix ships to browser>
- Bundle check: <how to inspect the built client bundle for leaked keys>

## Authentication
- Verified identity comes from: <the safe accessor - token/session helper>
- The "never trust identity from args/body" tell in this stack: <…>
- Prod vs test instance check: <…>

## Authorisation & tenant isolation
- A tenant is a: <org | team | user>
- Tenant key field: <`orgId` / `tenant_id` / …>
- **Scoping seam (the target):** <the single shared wrapper/guard/RLS policy every data path routes through>
- Audit = find every data path on the surface that does NOT go through the seam.

## Public vs internal surface
- "The surface" = enumerate: <routes / all public server functions / resolvers / actions>
- "Not public" mechanism: <middleware / internal tier / separate service>
- Known places debug/admin/seed routes hide: <…>

## Input validation
- Validation lives at: <schema validators on the boundary - name the lib>
- Injection risk surface: <raw SQL? template rendering?>

## Cost & abuse
- Paid-call surface to gate: <which handlers hit paid APIs>
- Rate-limit / entitlement layer: <…>
- Spend cap mechanism: <…>

## Data integrity & critical-path races
- Money/entitlement paths needing idempotency: <…>
- Webhook idempotency + signature method: <…>
- Atomic-op / transaction mechanism for counters/balances: <…>

## Backups, env separation, observability
- Backups + tested restore: <…>
- Prod/dev separation: <…>
- Rollback path: <…>
- Error tracking + structured logging on money/identity paths: <…>

## Gate defaults for this stack
<any stack-specific must-be-true items to fold into GATE.md beyond the standard line>
