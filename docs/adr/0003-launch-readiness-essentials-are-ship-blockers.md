# Missing backups, rollback, and error tracking are Holes, not Hardening

These aren't vulnerabilities, so a security reader would expect them in the Hardening bucket. They're classed as Holes (launch-blocking) anyway: shipping without them means the first production incident is unrecoverable and invisible, a launch-day catastrophe in the same class as a breach.

Recorded because the classification is surprising without this rationale.
